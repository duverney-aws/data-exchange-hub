i need an aws architecture diagram using aws services that show a self-service portal to onboard external pharma CMO partners to register the portal and register their data schemas and establish a data contract. it uses AWS Amplify, API gateway, Lambda for portal logic, glue schema registry for CMOs to register their schemas, a dynamodb to store data contracts, three ingestiong patterns pattern 1 include glue connectors, pattern 2 AWS transfer family, pattern 3 textract and rekognition, a data lake medallion architecture bronze layer, silver layer, gold layer, a processing analytics stack including AWS glue etl jobs, Amazon athena, amazon Bedrock. a governance layer wuth lake formation, monitoring and orchestration with cloudwatch, eventbridge, step functions. and an API layer for internal systems to integrate with the system showing ERP (SAP), QMS (Veeva), LIMs, Redshift, Quick Suite. see key flows: 1. CMO ONBOARDING (One-time, 30 minutes):

   CMO → Portal → API Gateway → Lambda → Schema Registry + Data Contracts → Step Functions → Pipeline Deployed

2. DATA INGESTION (Ongoing, automated):
   CMO Data → Pattern 1/2/3 → Bronze Layer → Validation → Silver Layer → Aggregation → Gold Layer

3. DATA CONSUMPTION (Self-service):
   Gold Layer → Athena (SQL) → Merck Systems
   Gold Layer → Bedrock (Gen AI) → QuickSight Dashboards

4. GOVERNANCE (Continuous):
   All Data → Lake Formation (Access Control) + KMS (Encryption) + CloudTrail (Audit)